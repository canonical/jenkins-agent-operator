name: Integration tests

on:
  pull_request:

jobs:
  integration-tests:
    strategy:
      matrix:
        runtime: [ s390x, ppc64le, x64, arm64 ]

    permissions:
      contents: read
      packages: write

    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@fix/skip-check-allure
    with:
      pre-run-script: |
        -c "sudo microk8s config > ${GITHUB_WORKSPACE}/kube-config
        chmod +x tests/integration/pre_run_script.sh
        ./tests/integration/pre_run_script.sh"
      juju-channel: 3.5/stable
      channel: 1.34-strict/edge # s390x and ppc64le architectures are only available on edge
      builder-runner-label: ${{ matrix.runtime }}
      self-hosted-runner: true
      self-hosted-runner-arch: ${{ matrix.runtime }}
      self-hosted-runner-label: edge
      tmate-debug: true
      tmate-timeout: 90
